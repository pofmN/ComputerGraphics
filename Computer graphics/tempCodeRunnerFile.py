boundary_fill(x + 1, y + 1, fill_color, boundary_color)
        # boundary_fill(x - 1, y + 1, fill_color, boundary_color)
        # boundary_fill(x + 1, y - 1, fill_color, boundary_color)
        # boundary_fill(x - 1, y - 1, fill_color, boundary_color)